from settings import *

# Mappings of unique chars to integers, and a reverse mapping
char_to_int = dict((c, i) for i, c in enumerate(ALPHABET))
int_to_char = dict((i, c) for i, c in enumerate(ALPHABET)) 
